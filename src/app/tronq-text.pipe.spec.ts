import { TronqTextPipe } from './tronq-text.pipe';

describe('TronqTextPipe', () => {
  it('create an instance', () => {
    const pipe = new TronqTextPipe();
    expect(pipe).toBeTruthy();
  });
});
